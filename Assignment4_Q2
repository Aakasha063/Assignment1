#Python program to triple all numbers of a given list of integers. Use Python map

given_list = []

n = int(input('Enter the number of elements you want in list :'))

for i in range(1,n+1):
    element = int(input(f'Enter number {i} to add in list :'))
    given_list.append(element)

print(given_list)

def triple(x):
    return x*3

tripled_list = map(triple, given_list)

print('The tripled numbers of our list are :',list(tripled_list))
